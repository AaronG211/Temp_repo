# Doodle Arena: OnGUI/List-based to Real Unity Objects Conversion

## Instructions for Claude (read this first)

This is a temp working copy of a Unity project, not anyone's live repo. Your job is to convert the whole thing from its current OnGUI/plain-C#-list architecture to a real Unity scene/prefab/component architecture, following the patterns already demonstrated elsewhere in this project in the Q1 and Q2 folders (UFOController's transform.position movement, RotatingShipController's Rigidbody2D + AddTorque/AddRelativeForce, MazeObstacle's OnCollisionEnter2D, and the SerializeField-exposed tuning values on both).

Four people will each pull one piece of this back into their own copy of the main repo afterward: Aaron takes the shared game state and boss, Sideesh takes the player, Yipeng takes the enemies, Aijia takes the presentation/HUD. Because of that, keep each person's converted work in its own file(s) named after what they already have (DoodleArenaGame.cs / GameManager.cs, DoodleArenaBoss.cs, DoodleArenaPlayer.cs, DoodleArenaEnemies.cs, DoodleArenaPresentation.cs), don't merge two people's logic into one file just because they both reference GameManager, and don't rename or restructure folders beyond what's needed for the conversion. Someone needs to be able to open this temp project, find the one file or prefab set they need, and copy it out.

Work in this order, since later pieces depend on the shared state existing first:

1. Build the GameManager singleton first. This replaces the partial-class DoodleArenaGame.cs pattern where everything reached into shared fields (enemies, arena, shake, hitStop, combo, state, playerHp). Keep the same method/field names where they already exist (HurtPlayer, DamageEnemy, Burst, shake, hitStop, combo, state) so the conversion of everything else is mechanical, just expose them via GameManager.Instance instead of a partial class. This is the piece Aaron will own.
2. Convert Enemy, Shot, and Spark from plain private C# classes into real MonoBehaviours on actual prefabs (EnemyController, ProjectileController, SparkController, etc.), each with a SpriteRenderer + Collider2D, and Rigidbody2D where movement needs physics. Give the boss its own prefab with its phase logic living on that prefab's script instead of the current UpdateBoss(Enemy boss, ...) method reaching into a shared list. Replace the List<Enemy>/List<Shot> bookkeeping with Instantiate/Destroy and Unity's own OnTriggerEnter2D/OnCollisionEnter2D callbacks.
3. Convert DoodleArenaPlayer.cs: move the player off a private Vector2 playerPos field and onto transform.position (or a Rigidbody2D) on a real player GameObject with a SpriteRenderer and Collider2D. Replace NearestEnemy() + manual Vector2.Distance checks with a real Collider2D, a trigger zone in front of the player for melee (PrimaryAttack), OnTriggerEnter2D on the projectile itself for ranged. Pull every hardcoded number (22 damage, .32f cooldown, 105 melee range, dash distance, etc.) out into [SerializeField] fields. Route DashAttack and HurtPlayer's calls into enemies/shake/hitStop/combo/state through GameManager.Instance instead of direct field access.
4. Convert DoodleArenaEnemies.cs: give each enemy kind (Chaser/Shooter/Tank/Boss) its own prefab, or one prefab with a kind field driving behavior, with Rigidbody2D + Collider2D. Replace SpawnEnemy's list-append with a real Instantiate(enemyPrefab, position, rotation) call. Remove the manual pairwise separation double-loop and let Unity's physics resolve collisions between enemy Rigidbody2Ds. Replace FireEnemyShot's list-append with instantiating a real projectile prefab. Pull HP formulas, spawn timing, and movement speeds into [SerializeField] fields. Route damage/death/scoring calls through GameManager.Instance.
5. Convert DoodleArenaPresentation.cs plus the HUD code still in DoodleArenaGame.cs: build a real Canvas with Image, Text, and Slider/fill-Image components for health bars, score, round/wave label, banners, and the title/game-over/victory screens, replacing every GUI.DrawTexture/GUI.Label/GUI.color call. Make screen shake actually move the Camera's transform instead of nudging GUI.matrix. Replace the hand-rolled List<Spark> Burst effect with a real ParticleSystem component. Delete DrawPlayer, DrawEnemy, DrawShot, DrawCircle, and DrawRotated entirely, since that visual work now lives on the sprites assigned to the prefabs from steps 2-4. Have the HUD read playerHp, combo, and score from GameManager.Instance.

Beyond the C# scripts: since a script alone doesn't create a scene hierarchy, also do the actual scene/prefab setup where you can, create the prefab assets, add the components, wire up serialized references (assign a placeholder sprite is fine if there's no art yet), and set up the Canvas and its child UI elements in the scene, not just describe what should exist. Where a wiring step is normally a manual Inspector action (dragging a reference between two prefabs, assigning a sprite), write an editor script to do it instead, following the pattern already in this project's WebGLBuildHelper.cs (an Editor-folder script with a MenuItem), so the setup is reproducible and visible in a file rather than trapped in a manually-clicked state that doesn't show up in a diff. If something genuinely can't be done outside the Editor UI, leave a clear TODO comment naming exactly what still needs a human to click, rather than silently skipping it.

When you're done, give a short summary of what changed per file/person so whoever picks this apart later knows exactly what to pull.

## Why this conversion is happening

The current build works, but almost none of it uses real Unity constructs. There's no scene hierarchy, no prefabs, no colliders, no Rigidbody2D, and no Canvas. Everything lives inside one MonoBehaviour's Update() and OnGUI(), with "objects" like Enemy and Shot being plain private C# classes tracked in Lists rather than actual GameObjects.

## Aaron: DoodleArenaGame.cs (shared core) + DoodleArenaBoss.cs

What's there now: DoodleArenaGame.cs holds all shared state as plain fields on the MonoBehaviour, playerPos, a List<Enemy>, a List<Shot>, sparks. Enemy and Shot are private sealed classes, not Unity objects. There's no scene hierarchy at all, DoodleArenaBootstrap.cs spawns one empty GameObject at runtime and everything happens inside that single component's Update()/OnGUI(). The boss (DoodleArenaBoss.cs) is just a method, UpdateBoss(Enemy boss, Vector2 direction, float dt), operating on that same plain Enemy class, no boss GameObject exists anywhere.

What needs to change: Enemy, Shot, and Spark stop being plain C# classes and become real MonoBehaviours attached to actual prefabs (EnemyController, ProjectileController, etc.), each with a SpriteRenderer + Collider2D (+ Rigidbody2D for physics-based movement, matching Q2's ship). The boss becomes its own prefab with its phase logic living on that prefab's script instead of a method reaching into a shared list. The List<Enemy>/List<Shot> bookkeeping goes away in favor of Instantiate/Destroy and Unity's own collision callbacks.

## Sideesh: DoodleArenaPlayer.cs

What's there now: UpdatePlayer moves the player by directly adding to a Vector2 playerPos field every frame, no Transform, no Rigidbody2D. Closest existing analog is Q1's UFOController, except UFO actually moves transform.position and this moves a private math variable that then gets drawn at that position via OnGUI. PrimaryAttack/DashAttack find targets and register hits using NearestEnemy() + manual Vector2.Distance(...) < radius checks, no colliders at all. Attack numbers (22 damage, .32f cooldown, 105 melee range, etc.) are hardcoded inline.

What needs to change: the player becomes an actual GameObject with a SpriteRenderer and Collider2D, movement switches to modifying transform.position (or a Rigidbody2D) directly instead of a separate tracked variable, and hit detection switches from NearestEnemy() + distance math to a real Collider2D. All the numeric constants become [SerializeField] fields.

The architecture decision already made: DashAttack and HurtPlayer used to reach directly into shared fields on the partial class DoodleArenaGame (enemies, arena, shake, hitStop, combo, state). Going with a lightweight GameManager singleton for this rather than an event-driven approach, since it's the smaller change relative to what everyone already has and keeps one obvious place (GameManager.Instance) for the HUD and enemy scripts to read/write through instead of everyone wiring up event subscriptions under time pressure.

## Yipeng: DoodleArenaEnemies.cs

What's there now: UpdateEnemies loops a List<Enemy> every frame, switches on enemy.kind (Chaser/Shooter/Tank/Boss) to decide movement math, and does its own enemy-vs-enemy separation math via a nested double for loop checking every pair's distance. SpawnEnemy creates a plain Enemy object and adds it to the list, nothing is ever actually instantiated in the scene. FireEnemyShot does the same thing for enemy projectiles.

What needs to change: each enemy type becomes its own prefab (or one prefab with a kind field driving behavior) with Rigidbody2D + Collider2D. SpawnEnemy becomes an actual Instantiate(enemyPrefab, position, rotation) call. The manual pairwise separation loop gets replaced by Unity's physics doing collision resolution between enemy Rigidbody2Ds automatically. FireEnemyShot instantiates a real projectile prefab instead of appending to a list. All stat numbers become [SerializeField] fields.

## Aijia: DoodleArenaPresentation.cs + the HUD portion still in DoodleArenaGame.cs

What's there now: literally every visual in the game, the paper-texture background, the title screen text, the player/enemy/shot doodle-circle drawings, health bars, banners, game-over/victory screens, is a GUI.DrawTexture/GUI.Label/GUI.color call inside OnGUI. Screen shake is faked by nudging GUI.matrix each frame. Particles (Burst/Spark) are a hand-rolled List<Spark> drawn as little rotated rectangles, not a real particle system.

What needs to change: the entire HUD becomes a Unity Canvas with real Image, Text, and Slider/fill-Image components. Screen shake becomes actually moving the Camera's transform. Burst's hand-drawn sparks become a real ParticleSystem component. The procedural drawing methods (DrawPlayer, DrawEnemy, DrawShot, DrawCircle, DrawRotated) go away entirely, since that visual work moves onto the sprites assigned to the prefabs built in the steps above.
